/**
 * Pagination config (centralized to avoid scattered magic numbers)
 */
export const MY_GALLERY_PAGE_SIZE = 6
export const PUBLIC_GALLERY_PAGE_SIZE = 6

// For SEO bots/crawlers: render more items on the first screen
export const PUBLIC_GALLERY_SSR_BOT_PAGE_SIZE = 12
